[workspace]

members = [
    "measure",
    "orchestrator",
]

default-members = [
    "measure",
    "orchestrator",
]

resolver = "2"


[workspace.dependencies]
tokio = { version = "1.36.0", features = ["full"] }
serde = { version = "1.0.196", features = ["derive"] }